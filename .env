# Default environment variables
# These can be overridden by .env.local, .env.development, or .env.production

# Enable mocks in development by default (this is redundant as our code enables it by default)
# But it's good to have it explicitly defined for clarity
VITE_ENABLE_MOCKS=true
